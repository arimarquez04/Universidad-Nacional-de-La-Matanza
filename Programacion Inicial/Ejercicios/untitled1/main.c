#include <stdio.h>

int main(void)
{
    printf("Hello, World!\n");
    /*
     * a)Una empresa exportadora envia sus productos a distintos destinos, los cuales tienen el siguiente código: (L-Local, B-Brasil, P-Paraguay y U-Uruguay).
     * La empresa necesita un programa que registre sus actividades y realice ciertos informes.
     * Para ello cuenta con la siguiente información que se ingresa por teclado.
     * CodProducto (entero que tiene 3 cifras) Destino (char (Solo estas letras: L,B,P y U)
     * Cantidad (entero mayor a 0 y menor a 100) Precio  (flotante mayor a 0 y menor a 100000) Todos los datos deben ser validados.
     * La carga de los datos termina con un código de producto igual a 0. Se pide al final informar:
     * a) El destino que mas productos vendio.
     * b) El pais con menor importe. c) El promedio del importe en el mercado Local.
     * d) Realizar el siguiente informe:                            Informe de Ventas                          Destino      cantidades   importe                          xxxxxx         xxxxx       xxxx.xx                          xxxxxx         xxxxx       xxxx.xx
     */

    /*printf("ingrese un numero flotante\n >>");
    float numero =0;
    scanf("%f", &numero);
    printf("numero: %.2f", numero);*/
    int codigoDeProducto = 0;
    printf("aaaaaaa \t dddddd");

    return 0;
}
